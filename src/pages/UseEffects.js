import React, { Component, useEffect } from "react";
import axios from "axios";

//UseEffect: It's a STATE which TRIGGERS/RUNS on EVERY RENDERS.

export default function UseEffects(){
    
    return(
        <>
        </>
    );
}